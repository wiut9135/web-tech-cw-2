extends layout.pug 
block number1 
    if error 
     p space is empty please fill the input
    form.form(action='/add' method='POST')
        input.input(type='text' placeholder='Attach your todo here...' name='todo')
        button.btn Add

    
    .todos 
     if todos.length
         each todo in todos
             p.todo 
                 span #{todo.description}
                 a.delete(href=`/${todo.id}/delete`) Delete
     else 
         p.todo No any records   